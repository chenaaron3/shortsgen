/**
 * DB table schemas (runs, videos). Re-exported from @shortgen/db (drizzle-zod).
 * Used for API validation and Python Pydantic codegen via types:sync.
 */

export { runSchema, videoSchema, type Run, type Video } from "@shortgen/db";
